spring:
  application:
    name: showtimes-service
  profiles:
    active: dev
server:
  servlet:
    context-path: /api/v1
  port: 9090
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9010/eureka